{% extends 'base.html.twig' %}

{% block content %}
    <h1>Edit bankaccounts</h1>

    <div class="page-body-block">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit">Save</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
